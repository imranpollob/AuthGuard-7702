# Risk Register — AuthGuard-7702 Revision v2

Probability: L/M/H. Impact: scientific consequence if realized. Every risk has a decision
point tied to the master plan's gates.

| # | Risk | Prob | Scientific impact | Mitigation | Fallback | Decision point |
|---|---|---|---|---|---|---|
| R1 | Corrected (inner-OOF) threshold selection materially changes G-DET F1/P/R or G-MUT/G-VOL recalls | M | Operating-point story changes; mutation-robustness ordering could shift. AUPRC headline unaffected (threshold-free). | Frame as threshold-transfer validity, not error correction; report v1→v2 deltas transparently in a reconciliation table (pattern already exists: `original_vs_task_aligned.md`) | Report corrected numbers as the paper numbers; keep v1 frozen for audit | End of Phase 1B: if authguard M3 retained-recall changes sign vs baselines, re-scope RQ3 wording before Phase 3 |
| R2 | Donor-isolated regeneration shrinks or eliminates the F200 augmentation gain (0.484→0.727 may be donor-specific) | M-H | RQ4 headline weakens; abstract sentence must change | Multi-donor partition-isolated pools raise external validity; paired family-clustered CIs decide significance; compound M3+F200 evaluated at the same time | Demote augmentation to a secondary observation; promote evaluation-framework + (if passed) Gate A/B contribution; report v1 result as donor-confounded in internal report | End of Phase 1C: CI on donor-isolated Δrecall(F200) excludes 0 → keep claim; else rewrite RQ4 |
| R3 | A stronger baseline (TF-IDF LR/SVM, hash-XGB) matches or beats AuthGuard 0.881 | M | "Lightweight model outperforms evaluated baselines" claim falls; model contribution demoted | Identical folds/thresholds/seeds for fairness; paired family-clustered ΔAUPRC CI; ablations explain *why* | Reposition paper around task-aligned dependence-aware evaluation + robustness analysis; report the stronger baseline as the recommended simple model | End of Phase 3A: if paired CI(AG − best baseline) includes 0, rewrite contribution 2 before manuscript integration |
| R4 | Family recomputation (post-alignment) or 0.75/0.90 grouping changes conclusions | L-M | Primary frozen-family result questioned as clustering-dependent | Frozen families remain the outcome-blind primary; alternatives are labeled sensitivity analyses; report chaining diagnostics to explain divergences | Report sensitivity honestly; if 0.90 (stricter) shrinks the random-vs-family gap, the leakage narrative still holds directionally | End of Phase 3C: sensitivity table reviewed before manuscript claims about generality of the gap |
| R5 | Dual-view representation (Gate A) fails its predefined success criteria | M-H | No new technical-novelty contribution from Gate A | Criteria frozen in `revision_v2/protocols/gateA_success_criteria.md` BEFORE results; time-boxed 4 working days; trivial first-STOP heuristic run first as the bar | Fall back to Gate B; if both fail, contribution structure = task-alignment + evaluation framework; failures documented in internal report only | Gate A checkpoint at end of Phase 4A |
| R6 | Selective escalation fails to concentrate errors | M | No Gate B contribution; escalation section dropped | Use multiple independent signals (conflict history, trailing ratio, margin, outlier score); freeze rule on train/val only; time-boxed 3 working days | Report as negative internal result; keep abstention-for-known-conflicts as a small honest paragraph if it alone works | Gate B checkpoint at end of Phase 4B |
| R7 | No execution harness exists; building one stalls (entry points unknown, forks needed, foundry unavailable offline) | H | Phase 2C semantic-validity evidence unavailable | Feasibility spike first (0.5 day) on 2 contracts before committing; use anvil fork replay of recorded transactions where the USENIX artifact lists attack txs | Deliver "minimum implementation needed" report; manuscript keeps syntactic-only scoping (already worded correctly) | Phase 2C go/no-go after spike |
| R8 | Gigahorse/Soufflé reproduction blocked (ARM macOS, Docker, dependency rot) | M-H | No reference-analyzer comparison; motivation must avoid speed claims | Hard time-box 1–2 working days; use upstream Docker image; USENIX intermediate outputs (`detect_result.jsonl`) allow limited agreement analysis WITHOUT running the toolchain | Option B report (blockers, attempted steps, remaining-work estimate); manuscript positions systems as complementary — already the current wording, so no claim removal needed | Phase 5 fork decision at time-box expiry |
| R9 | Insufficient human reviewers for label adjudication | H (short-term) | Issue 1 stays partially open; no inter-rater stats | Package (Phase 2B) is deliverable without reviewers; agreement scripts pre-tested on synthetic labels | Paper states adjudication package prepared, adjudication pending; keeps INSUFFICIENT DATA verdict wording | Before manuscript freeze: include agreement stats only if ≥2 reviewers completed |
| R10 | Dataset licensing / artifact-release restrictions (USENIX-derived bytecodes, blacklist lists, LFS-hosted CSVs) | M | Anonymous artifact cannot include the corpus | Check upstream licenses early (Phase 0 audit task); bytecode is on-chain public data but the *label* compilation may carry artifact terms | Deterministic reconstruction instructions + SHA-256 manifest + fetch scripts (pattern already exists in `task_alignment.py --network`) | Phase 6A packaging decision |
| R11 | Changed numbers cascade into manuscript inconsistencies (hand-typed tables) | H if unmanaged | Silent number drift — the exact failure mode the audits guard against | Build table-generator scripts in Phase 1 so every .tex table regenerates from JSON; re-run `final_number_audit` procedure at Phase 6C | Manual audit pass as before | Phase 6C entry criterion: zero hand-typed result numbers |
| R12 | Frozen-artifact contamination by accident (rerunning `run_all.sh` or `task_aligned_rerun.py` overwrites frozen outputs in place) | M | Loss of frozen evidence lineage | Phase 0: revision branch + all v2 code writes ONLY under `revision_v2/`; add a guard checksum script that verifies frozen-file hashes before/after each experiment run (hashes already listed in `task_aligned_result_provenance.md`) | Git history + LFS recovers files; provenance hashes detect drift | Every phase report includes a frozen-hash verification block |
| R13 | Compute/runtime surprises (G-ADV v2 with multi-donor pools and repeated seeds is the heaviest job) | L-M | Schedule slip | Estimates in experiment matrix are CPU-scale (largest ~2–3 h); parallelize by fold if needed | Reduce seeds (3→1) for exploratory arms, keep 3+ for reported numbers | Phase 1C mid-run check |
| R14 | Threshold-protocol change makes G-ADV v1-vs-v2 comparison ambiguous (val-fold vs inner-OOF) | M | Reviewer confusion about which protocol applies where | Run G-ADV v2 under BOTH its original val-fold thresholds and the unified inner-OOF variant; report the primary as the unified one; document the other in the appendix/artifact | Keep val-fold protocol (already defensible) and explain the difference explicitly | Phase 1C protocol freeze before running |
| R15 | benign_general secondary control shows high FPR at corrected thresholds | L-M | Operational story weakens (current original-cohort evidence: 1.3% flagged — favorable) | Report as measured with CIs; tie to operational analysis (alerts/1000) so the number has context | Discuss threshold choice trade-off; recommend higher-precision operating point | Phase 2D review |
